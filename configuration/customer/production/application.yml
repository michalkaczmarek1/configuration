spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customers?serverTimezone=Europe/Warsaw&useSSl=False&allowPublicKeyRetrieval=true
    username: kodilla
    password: kodilla_password
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 9999

